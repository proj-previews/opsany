import React from 'react';
import ReactDOM from 'react-dom';
import ORouter from './route'
ReactDOM.render(
  <React.StrictMode>
    <ORouter/>
  </React.StrictMode>,
  document.getElementById('root')
);
